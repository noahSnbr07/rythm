export default interface Song {
  id: string;
  title: string;
  artist: string;
  explicit: boolean;
  bannerURL: string;
  audioURL: string;
  duration: number;
  release: string;
  published: string;
}
